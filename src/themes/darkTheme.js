import { createTheme } from "@mui/material";

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#204396",
    },
    secondary: {
      main: "#7d71ff",
    },
  },
});

export default darkTheme;
