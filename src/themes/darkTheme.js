import { createTheme } from "@mui/material";

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#1322aa",
    },
    secondary: {
      main: "#7d71ff",
    },
  },
});

export default darkTheme;
